name: epsilon_mobile
description: Epsilon 267 Mobile Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.6.22
  video_player: ^2.8.6
  http: ^1.2.1             # <-- Existing HTTP client
  uuid: ^4.4.0             # <-- Existing UUID generator
  shared_preferences: ^2.2.2
  wakelock_plus: ^1.1.1
  web_socket_channel: ^2.4.5  # <-- NEW: WebSocket client for real-time communication
  intl: ^0.20.2
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.7


flutter:
  uses-material-design: true
  assets:
    - assets/backstories/
    - assets/videos/
    - assets/fpv_graphics/
    - assets/fpv_stories/
